import matplotlib
matplotlib.use('tkagg')
import matplotlib.pyplot as plt

from sklearn import manifold, datasets
from skimage.measure import structural_similarity as ssim
import numpy as np
import itertools
import os, sys, cv2, re, math, csv


# replace the kNN, graph approach with a distance matrix,
# SSIM as the distance metric
# make sense????

def gen_distance_matrix():
    """
    """
    dm = []
    return dm



def pairwise_ssim(im1, im2):
    """
    """
    return ssim






# ======== main ==========

if len(sys.argv) != 2:
    print("Usage: frameworkpython dm_manifold.py <img dir>")

f_list = []

for f in os.listdir(sys.argv[1]):    # img dir as commandline arg
    if (f.endswith(".tif") or f.endswith(".png")):
        f_list.append(f)
# change working directory
os.chdir(sys.argv[1])

for i_tuple in itertools.combinations(range(len()), 2)

# csv persistance

# visualization
